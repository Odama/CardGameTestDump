using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Spell : Card {
    /*
     * Currently empty unless I add more mechanics.
     * Spell cards are basically minions with no stats at the moment.
     * Because minions have abilities on play, spell cards can load the
     * same way but without staying on the board after play.
     * 
     */
}